#include "main.h"

/**
 * cap_string - changes lowercase to uppercase
 * @s: pointer
 * Return: char
 */
char *cap_string(char *s)
{
	int i;

	1 = 0;
	while (n[i] != '\0')
	{
		if (n[i] >= 'a' && n[i] <= 'z')
			n[i] = n[i] - 32;
		i++;
	}
	return (n);
}
